# Расчётные зависимости.
# PyTorch с поддержкой CUDA ставится отдельно, см. README:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.0
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
# для запуска ноутбука
jupyter>=1.0
ipykernel>=6.0
# для тестов
pytest>=7.0
